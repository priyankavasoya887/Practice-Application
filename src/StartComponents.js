import {Text, View} from 'react-native';
import React, {PureComponent} from 'react';
import Route from './Redux/Router/Route';

export default class StartComponent extends PureComponent {
  render() {
    return <Route />;
  }
}
